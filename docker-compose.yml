services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kabin
      POSTGRES_PASSWORD: kabin
      POSTGRES_DB: kabin
    ports:
      - "5432:5432"
    volumes:
      - kabin_pg:/var/lib/postgresql/data

volumes:
  kabin_pg:
